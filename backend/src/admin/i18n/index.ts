import en from "./json/en.json" with { type: "json" }

// Custom admin translations. Keys here override the dashboard defaults —
// e.g. login.title / login.hint rebrand the sign-in screen for Delfee.
export default {
  en: {
    translation: en,
  },
}
