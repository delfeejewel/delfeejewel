# Staging storefront runtime env — copy to /opt/delfee/staging.env on the
# droplet. Only server-side runtime vars belong here; NEXT_PUBLIC_* are baked
# into the :staging image at BUILD time (see .github/workflows/deploy-staging.yml
# build-args, sourced from STAGING_* GitHub secrets) and are NOT read from this
# file at runtime.

COMING_SOON_MODE=false
MEDUSA_BACKEND_URL=https://api-staging.delfee.in
REVALIDATE_SECRET=
